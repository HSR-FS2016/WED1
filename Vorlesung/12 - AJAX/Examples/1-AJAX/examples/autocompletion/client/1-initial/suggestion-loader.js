$(document).ready(function() {
    var suggestionAPI = 'http://localhost:8080/suggestions?search=';
    var fromField = $('#from');
    var suggestions = $('#suggestions');

    // TODO: listen for 'input' event on from-field


			// TODO: if input > 3 characters, fetch suggestions from suggestionAPI+from-field value


});